## 2026-07-04 Task: init
Initialized notepad for fvm-aot-milestone-todos. Plan requires delegated execution only; Atlas must not edit product files.

## 2026-07-04T22:08:15Z Task: T01 clean baseline
Baseline validation passed before product edits. `git status --short --untracked-files=all` showed only `.omo/**` planning/runtime files, including `.omo/run-continuation/*.json`, all explicitly excluded from staging, deletion, cleanup, or product-edit scope.

Required command results: `GIT_MASTER=1 git status --short --untracked-files=all` exit 0, `cargo fmt --check` exit 0, `cargo test` exit 0, and `cargo clippy --all-targets -- -D warnings` exit 0. Full transcript is in `.omo/evidence/task-01-fvm-aot-milestone-todos.md`.

## 2026-07-04T22:31:37Z Task: T02 classfile split
Moved classfile frontend parsing into `src/fvm_aot/classfile.rs` while keeping evaluator/emitter code and public `compile_jar` in `src/fvm_aot/mod.rs`. `ClassFile::parse(b"nope")` behavior stayed covered by `fvm_aot::tests::rejects_invalid_classfile`; final `cargo fmt --check`, targeted parser test, full `cargo test`, and `cargo clippy --all-targets -- -D warnings` all passed. LSP daemon timed out, so cargo/clippy are the available code diagnostics.

## 2026-07-04T22:56:43Z Task: T03 unsupported diagnostics split
Extracted `unsupported_opcode_message` to `src/fvm_aot/diagnostics.rs` and JVM descriptor/type helpers to `src/fvm_aot/types.rs`; evaluator/emitter/classfile behavior stayed in `src/fvm_aot/mod.rs`. Baseline and final `cargo test unsupported_ -- --nocapture`, full `cargo test`, `cargo fmt --check`, and `cargo clippy --all-targets -- -D warnings` passed. Product commit: `24a32cd aot: isolate unsupported diagnostics`. LSP remained unavailable (`rust-analyzer` missing/timeouts), so cargo/clippy remain the reliable diagnostics.

## 2026-07-04T23:35:00Z Task: T04 evaluator extraction
Extracted build-time evaluator state and helpers into `src/fvm_aot/evaluator.rs`; `src/fvm_aot/mod.rs` now keeps `compile_jar`, class-world loading, C emitter, and tests. Baseline println/HTTP smoke tests and dynamic loading unsupported test passed before edits. Final `cargo test compiles_ -- --nocapture`, `cargo test unsupported_dynamic_class_loading_reports_required_feature -- --nocapture`, `cargo test`, `cargo fmt --check`, and `cargo clippy --all-targets -- -D warnings` passed. Product commit: `1fec2ca aot: isolate build-time evaluator`. LSP daemon timed out again, so cargo/clippy remain the reliable diagnostics. Evidence: `.omo/evidence/task-04-fvm-aot-milestone-todos.md`.

## 2026-07-04T23:42:12Z Task: T05 generated C emitter extraction
Extracted the stopgap generated C emitter into `src/fvm_aot/emitter.rs` with only `pub(super) emit_c` exposed back to `mod.rs`; generated C strings for native println and HTTP server were moved unchanged. Combined cargo smoke filter was rejected by Cargo, so the two filters were run separately before edits and passed. Final `cargo fmt --check`, both specific smoke filters, `cargo test compiles_ -- --nocapture`, full `cargo test`, and `cargo clippy --all-targets -- -D warnings` passed. LSP daemon still times out; cargo/clippy remain the reliable diagnostics. Evidence: `.omo/evidence/task-05-fvm-aot-milestone-todos.md`.

## 2026-07-04T23:59:00Z Task: T06 shared AOT fixtures
Extracted shared AOT test fixture mechanics into `src/fvm_aot/test_support.rs` under `#[cfg(test)]`: source writing, `javac --release 17`, JAR packaging, native compile/run, HotSpot baseline, HTTP wait/cleanup, missing-source error assertion, and artifact preservation hooks. Existing `fvm_aot::tests` still run as 13 focused tests with the same unsupported diagnostic substrings and exact native stdout/HTTP body expectations. Final `cargo fmt --check`, `cargo test fvm_aot::tests -- --nocapture`, full `cargo test`, and `cargo clippy --all-targets -- -D warnings` passed. LSP daemon still times out; cargo/clippy remain the reliable diagnostics.

## 2026-07-05T00:22:00Z Task: T07 current AOT slice
Moved the seven supported behavior fixtures into `src/fvm_aot/tests/current_slice.rs` via `mod current_slice`, leaving unsupported diagnostic tests in `src/fvm_aot/mod.rs`. `cargo test current_slice -- --nocapture` now runs the supported subset for println, computed HTTP, statics/clinit, objects/arrays, multi-class closed world, interface dispatch/string concat, and String/Object/array core methods. Missing `cc`/`javac` handling for supported fixtures now prints an explicit skip reason; a controlled PATH without `cc` passed with seven skip messages. LSP daemon still timed out, so final diagnostics rely on cargo, clippy, and rustfmt.

## 2026-07-05T01:21:01Z Task: T08 unsupported diagnostics
Moved unsupported diagnostics fixtures into `src/fvm_aot/tests/unsupported.rs` via `mod unsupported`; `cargo test unsupported -- --nocapture` now runs six module tests plus the pre-existing CLI unsupported filter match. The shared unsupported helper enforces at least two diagnostic substrings per fixture and at least one location or milestone context substring. A first switch QA fixture using `args.length` hit null-array evaluation before switch dispatch, and a two-case switch emitted `lookupswitch`; the final dense 0..5 switch reliably emits `tableswitch` (`opcode 0xaa`) and asserts `planned milestone: primitive-completeness`. Final `cargo fmt --check`, `cargo test unsupported -- --nocapture`, `cargo test fvm_aot::tests -- --nocapture`, `cargo test`, and `cargo clippy --all-targets -- -D warnings` passed. LSP daemon still timed out, so diagnostics rely on cargo/clippy/rustfmt.

## 2026-07-05T02:04:00Z Task: T09 differential stdout harness
Added `src/fvm_aot/tests/differential.rs` with a HotSpot-vs-native helper for non-HTTP stdout fixtures. The initial `AotHello` fixture compares HotSpot stdout, generated native stdout, and expected bytes exactly as `hello fvm-aot\n`; the negative `athrow` fixture asserts `compile_native(... dry_run: true)` fails with unsupported build diagnostics before any native binary execution. Final `cargo fmt --check`, `cargo test differential_println_matches_hotspot -- --nocapture`, `cargo test differential -- --nocapture`, `cargo test fvm_aot::tests -- --nocapture`, `cargo test`, and `cargo clippy --all-targets -- -D warnings` passed. LSP daemon still timed out at the shared socket, so cargo/clippy/rustfmt remain the reliable diagnostics.

## 2026-07-05T02:38:00Z Task: T10 failure artifacts
Added `src/fvm_aot/tests/failure_artifacts.rs` with child-process env isolation for `FVM_KEEP_FAILED_AOT`, so the tests do not mutate the parent process environment or race other tests. `AotFixture::preserve_failed_artifacts` now returns a retained path only when `FVM_KEEP_FAILED_AOT=1`, and panic-time retention is gated by the same env var instead of preserving by default. The controlled keep test writes representative Java source, class, JAR, native, and compiler-log artifacts under the fixture tempdir and removes the retained directory after assertion; the default cleanup test verifies no retained path is reported and the temp root is gone after drop. Final `cargo fmt --check`, `cargo test failure_artifacts -- --nocapture`, `cargo test fvm_aot::tests -- --nocapture`, `cargo test`, and `cargo clippy --all-targets -- -D warnings` passed. LSP diagnostics still timed out at `/Users/scichocki/.codex/codex-lsp/daemon/v0.1.0/daemon.sock`.

## 2026-07-05T03:15:00Z Task: T11 Firecracker AOT smoke
Added ignored integration coverage in `tests/aot_firecracker.rs` for the real `fvm-aot` Firecracker path. The test gates before doing any Java build on non-Linux, missing `/dev/kvm`, missing `/dev/net/tun`, missing `FVM_KERNEL`, missing Firecracker/Java/C compiler/network/rootfs tools, and prints actionable skip reasons instead of panicking. On Linux/KVM with the gate complete, it compiles all `examples/aot-http/src` Java sources using `javac`, packages the JAR with `jar`, uses `env!("CARGO_BIN_EXE_fvm")` for `fvm build --backend fvm-aot`, then runs `fvm run --once --wait-http /health` and `fvm inspect --verify`. Local macOS verification passed by explicit skip; no Linux/KVM success is claimed. LSP diagnostics still timed out at `/Users/scichocki/.codex/codex-lsp/daemon/v0.1.0/daemon.sock`; cargo/rustfmt/clippy passed.

## 2026-07-05T04:05:00Z Task: T12 perf-aot wrapper
Added `scripts/perf-aot` as the current AOT HTTP benchmark wrapper. It compiles all `examples/aot-http/src` Java sources, packages `aot-http.jar` with main class `AotHttp`, builds `aot-http.fvm` through `fvm build --backend fvm-aot`, runs the cold `/health` benchmark with `--benchmark-iterations "$ITERATIONS"`, and writes `timings.env`, `sizes.env`, `inspect.txt`, and `aot-math.txt` under `perf-results/aot-*`. The default guest port is `9090`, derived from the committed source path `Http.respond(responder.port(), responder.body())` via `AotConfig(9000, ...)` and offsets `{40, 50}`. Local macOS verification covered `bash -n`, `--help`, and a missing-kernel failure that exits 1 with an actionable message; no Linux/KVM benchmark success is claimed. Final `cargo fmt --check && cargo test && cargo clippy --all-targets -- -D warnings` passed.

## 2026-07-05T05:07:40Z Task: T13 runtime compiler IR model
Added the model-only runtime compiler IR in `src/fvm_aot/ir.rs` plus `src/fvm_aot/tests/ir.rs` for the empty-main snapshot and invalid branch target verifier path. The IR stays unwired from `compile_jar`; T14-T18 own lowering/codegen/reachability.

`src/fvm_aot/ir.rs` stays at 226 pure LOC by keeping tests in the existing AOT test module tree. It has a scoped `#![allow(dead_code)]` because this milestone intentionally introduces the typed model before future tasks add production users; `cargo clippy --all-targets -- -D warnings` passes with that scoped allowance.

Final verification passed: `cargo test ir_empty_main_snapshot -- --nocapture`, `cargo test ir_ -- --nocapture`, `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`. LSP diagnostics still timed out at `/Users/scichocki/.codex/codex-lsp/daemon/v0.1.0/daemon.sock`.

## 2026-07-05T06:20:00Z Task: T14 integer bytecode lowering
Added a private test-scoped `src/fvm_aot/lower.rs` straight-line lowerer for static integer methods. It lowers int constants, locals, arithmetic, `iinc`, `ireturn`, and `return` into the T13 IR model, including explicit `check_nonzero ... else trap divide_by_zero` IR before `idiv` and `irem`. `compile_jar` remains evaluator plus generated C; the lowerer is not wired into production output.

The HotSpot-backed fixture test now compiles `AotLowerInt`, runs it on HotSpot, parses the classfile, and asserts exact deterministic IR text for `static int arithmetic(int,int)`. The failure QA compiles a `long` fixture and confirms lowering returns a `primitive-completeness` unsupported diagnostic instead of panicking. Final verification passed: `cargo test lower_int_arithmetic_to_ir -- --nocapture`, `cargo test lower_ -- --nocapture`, `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`. LSP remains unavailable because `rust-analyzer` is missing/timeouts; cargo/rustfmt/clippy remain authoritative.

## 2026-07-05T07:45:00Z Task: T15 branch/basic-block lowering
Split the T14 straight-line lowerer into `lower/bytecode.rs`, `lower/metadata.rs`, `lower/method.rs`, and `lower/state.rs` while preserving `crate::fvm_aot::lower::lower_method_to_ir`. The lowerer now plans deterministic basic blocks from bytecode leaders, validates branch targets before IR success, emits explicit `branch`/`branch_if` terminators for integer branches, reference/null placeholder branches, and `goto`, and renders successor lists in IR text.

The HotSpot-backed branch fixture lowers a Java loop to seven blocks with the loop back edge `bb5 -> [bb1]:`, and also lowers an `Object` null check to `cmp_ref_is_non_null_placeholder`. Corrupt branch offsets are rejected with class/method/bci/opcode/target context. `compile_jar` remains unchanged and the lowerer stays test-scoped. Final verification passed: `cargo test lower_branches_to_blocks -- --nocapture`, `cargo test lower_int_arithmetic_to_ir -- --nocapture`, `cargo test lower_ -- --nocapture`, `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`; LSP diagnostics still timed out at the daemon socket, so cargo/rustfmt/clippy remain authoritative.

## 2026-07-05T08:40:00Z Task: T16 closed-world reachability skeleton
Added an inert `src/fvm_aot/reachability.rs` skeleton that starts from the app `main`, records deterministic reachable classes/methods/fields, includes `<clinit>` for reachable app classes, parses method/field descriptors through existing helpers, and follows only app-owned direct `invokestatic` calls. The reachability module is not wired into production `compile_jar`; evaluator plus generated C remains the production path until T18.

The new `reachability_direct_static_helper` fixture snapshots `AotReachability.main -> helper -> int` with `AotReachability.<clinit>()V` and `AotReachability.seed:I`, while an internal reachability error test preserves the same dynamic `Class.forName` required-feature wording. Final verification passed: `cargo test reachability_direct_static_helper -- --nocapture`, `cargo test unsupported_dynamic_class_loading_reports_required_feature -- --nocapture`, `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`. LSP remained unavailable due to daemon timeout / missing `rust-analyzer`, so cargo/rustfmt/clippy are authoritative.

## 2026-07-06T03:11:38Z Task: T17 IR verifier
Added a real `FunctionIr::verify()` implementation behind `src/fvm_aot/ir_verify.rs`, with small type/value helper modules to keep files under the 250 pure-LOC ceiling. The verifier checks missing branch and exception targets, lowered-order value definitions before uses, return value/type compatibility against the function descriptor-bearing label, and unsupported IR types/trap markers before codegen. `FunctionIr` now carries the method descriptor only for verifier diagnostics and later T18 pipeline use; `compile_jar` remains unchanged and still uses the evaluator plus generated C.

The verifier test module includes one valid fixture plus invalid missing branch target, use-before-definition, invalid return type naming `Verifier.returnMismatch()I`, and unsupported return type fixtures. Final verification passed: `cargo test ir_verify -- --nocapture`, `cargo test ir_ -- --nocapture`, `cargo fmt --check`, `cargo test`, and `cargo clippy --all-targets -- -D warnings`. LSP diagnostics were attempted on all changed Rust files but timed out at `/Users/scichocki/.codex/codex-lsp/daemon/v0.1.0/daemon.sock`, so cargo/rustfmt/clippy are authoritative. `.omo/run-continuation/*.json` remained unstaged and untouched.
